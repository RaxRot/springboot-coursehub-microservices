spring:
  application:
    name: enrollment-service

  datasource:
    url: jdbc:mysql://localhost:3306/enrolldb_ch?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 19649072Sever

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 5003

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
